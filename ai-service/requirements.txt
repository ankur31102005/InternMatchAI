# ── Core ──────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.28.1
loguru==0.7.3

# NOTE: Heavy ML libraries (torch, sentence-transformers, lightgbm,
# shap, scikit-learn, numpy, pandas) were removed because the current
# service code does not import or use them. Re-add them here if/when
# the AI endpoints start using real ML models.
