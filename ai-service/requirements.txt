# ── Core ──────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.28.1
loguru==0.7.3

# ── Embeddings (Phase 1: semantic matching) ───────────────────
# CPU-only torch keeps the image smaller. sentence-transformers pulls in
# transformers + huggingface; the all-MiniLM-L6-v2 model (~90MB) is
# downloaded on first /embed call.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
sentence-transformers==3.0.1
